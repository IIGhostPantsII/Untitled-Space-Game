using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LowGrav : MonoBehaviour
{
    /*
    [SerializeField] private PlayerController playerScript;
    [SerializeField] private float gravity;
    [SerializeField] private float jumpForce;

    private float tempGrav;
    private float tempJumpForce;

    private void OnTriggerEnter(Collider other)
    {
        if(other.CompareTag("Player"))
        {
            tempGrav = playerScript.gravity;
            tempJumpForce = playerScript._jumpForce;
            playerScript.gravity = gravity;
            playerScript._jumpForce = jumpForce;
        }
    }

    private void OnTriggerExit(Collider other)
    {
        if(other.CompareTag("Player"))
        {
            playerScript.gravity = tempGrav;
            playerScript._jumpForce = tempJumpForce;
        }
    }
    */
}
